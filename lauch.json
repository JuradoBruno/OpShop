{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Attach to url with files served from ./out",
            "type": "chrome",
            "request": "attach",
            "port": 4200,
            "url": "<http://localhost:4200/>",
            "webRoot": "${workspaceFolder}/out"
        }
    ]
}